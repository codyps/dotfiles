# Alsa config (asoundrc) for the ao725
# Builtin audio chosen as default

# Note: plug is required for gnash w/o gstreamer.

pcm.!default {
	type plug
	slave.pcm "hw:1,0"
}

ctl.!default {
	type hw
	card 1
}
